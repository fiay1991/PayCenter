package com.park.paycenter.dao;

import org.springframework.stereotype.Repository;

import com.park.paycenter.domain.External_log_records;

/**
 * @author wangfanfan
 * 时间：2016年9月20日
 * 功能：支付中心-外部接口调用记录
 * 备注：
 */
@Repository(value = "External_log_recordsDao")
public interface External_log_recordsDao {

	/**
	 * 插入
	 * @param external_log_records
	 * @return
	 */
	Integer add(External_log_records external_log_records);
}
